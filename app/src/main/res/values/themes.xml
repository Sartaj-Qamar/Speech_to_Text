<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SpeechToText" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowActionBar">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>

    </style>

    <style name="Theme.SpeechToText" parent="Base.Theme.SpeechToText">
        <item name="android:statusBarColor">@color/black</item>

    </style>

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/transparent</item>
        <item name="android:background">@color/transparent</item>
    </style>
</resources>